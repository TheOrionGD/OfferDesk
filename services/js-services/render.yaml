services:
  - type: web
    name: offerdesk-backend-service
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5001
      - key: MONGODB_URI
        sync: false
      - key: AI_SERVICE_URL
        sync: false
